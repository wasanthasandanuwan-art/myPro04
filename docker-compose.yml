version: "3.9"

services:
  backend:
    build:
      context: ./backend2
    ports:
      - "5000:5000"

  frontend:
    image: nginx:alpine
    ports:
      - "8082:80"
    volumes:
      - ./frontend2:/usr/share/nginx/html